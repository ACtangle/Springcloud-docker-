server:
  port: 8031

spring:
  application:
    name: microservice-hystrix-turbine
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

#turbine:
#  appConfig:
#    microservice-consumer-movie,microservice-consumer-movie-feign-hystrix-fallback
#  cluster-name-expression: "'default'"
